# 橙区三项影响评估（决策表 #5 / #11 / #15，只评估不施工）

- 日期：2026-07-11　|　按 Owner「backlog 提前触发」裁定出评估；三项均改 contracts/主权面（橙），且 #15 缺证据——**本轮只交影响清单与建议，不动任何 schema/代码**。

## #5 LinkType 一等公民（Palantir 源）

- **要做的事**：把对象间关系从 05 现有 RelationEdge 升为类型化一等公民（LinkType 定义 + 双向导航 + 权限随链接）。
- **契约影响**：新契约对象（schema+Go 双轨）+ 05 真相源新表/索引 + `/v3` 查询端点 + 前端投影——**橙偏红**（动数据模型主干）。
- **兼容风险**：与 RelationEdge 共存 = 关系表达双真相源；迁移 = 一次性数据手术。
- **成本粗估**：contracts 1-2 天 + os 3-5 天 + client 2 天 + 迁移与回归。
- **证据现状**：缺。智能家居跑穿与既有四 pack 均未出现「类型化双向链接导航」需求；RelationEdge 尚未用满。
- **建议**：维持 backlog。触发条件 = 客户真实提出「从人看所有事 / 从事看所有人」级导航且 RelationEdge 表达不了；届时先做 contracts 形状评审再谈实装。

## #11 RiskType 五件套判事策略骨架字段（BKN 源）

- **要做的事**：场景荚判事策略从自由文本（`formalization_requirement.summary`）升为结构化 `risk_types[]`（定义 / 触发条件 / 证据要求 / 升级路径 / 回退）。
- **契约影响**：scene-pack-spec / pack-manifest additive 字段——橙（additive 但改治理声明形状）。
- **核心教训（必须写进任务卡）**：KWeaver RiskType 就是"脚手架无实现无调用方"（考古四实锤②）。**实装必须遵守规格与运行时同轮接线铁律**：同轮给 Base Gate / ImpactRiskGate 一个真实消费点（如 declared delete + risk_type 匹配时改升级路径），否则宁可不加字段。
- **成本粗估**：契约+双向突变守卫 1 天；Base 消费点 1-2 天。
- **建议**：backlog。触发条件 = 装修 Pack 作者轮真实作者写判事策略时，字段需求被作者行为验证（哪五件真的会填、哪些永远空着）。

## #15 per-agent 独立身份 token 审计粒度（DIP t_digital_employee.app_id 源）

- **要做的事**：给每个智能体独立身份/token，使 03 审计能归因到具体 agent。
- **契约影响**：16（身份签发）+ 03（actor 粒度）+ 13（agent 注册）——**红边橙**（动身份与审计主干）。
- **证据现状**：**缺**。且当前主体模型 = 秘书长单主体 + 顾问视角（非六独立 agent），粒度需求天然弱；候选/回执已带 `role_pack_ref` / `slot_ref`，多数归因场景够用。
- **低成本替代（现在就有）**：审计归因先用 role_pack_ref + slot_ref + task_purpose 三元组，不新发身份。
- **建议**：维持 backlog（三项中最不该提前做的一项）。触发条件 = 场景荚多角色对照大规模使用 + 客户明确的 per-agent 审计合规要求，两者同时成立。

## 汇总建议

三项本轮均**不实装**：#11 等作者轮验证（最可能先触发）、#5 等客户导航证据、#15 双条件都缺。若 Owner 仍裁定任一项现在实装，按各自「核心教训/兼容风险」条款先出实施卡再开工。
