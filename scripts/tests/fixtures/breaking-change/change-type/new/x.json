{
  "type": "object",
  "properties": {
    "id": {"type": "string"},
    "status": {"type": "string", "enum": ["draft", "sent", "paid"]},
    "amount": {"type": "string"}
  },
  "required": ["id"],
  "additionalProperties": true
}
