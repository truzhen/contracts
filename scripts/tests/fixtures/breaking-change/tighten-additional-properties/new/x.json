{
  "type": "object",
  "properties": {
    "id": {"type": "string"},
    "status": {"type": "string", "enum": ["draft", "sent", "paid"]},
    "amount": {"type": "integer"}
  },
  "required": ["id"],
  "additionalProperties": false
}
